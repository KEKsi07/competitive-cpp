# competitive-cpp
A complete project setup that combines CMake and GoogleTest for local competitive programming practice.

The project can now be built using the following commands:

1. Configure the build (only once)

`cmake -S . -B build`


2. Build the project

`cmake --build build`


3. Run the tests

`ctest --test-dir build`

